/target
.DS_Store
*.swp
*.swo
# Default server data dir (POWDB_DATA) — also matches nested copies
# such as clients/ts/powdb_data/ created by client integration runs.
**/powdb_data/
node_modules/
clients/ts/dist/
clients/sync/dist/

# Worker worktrees and tooling state
.claude/
.playwright-mcp/

# Bench artifacts
crates/compare/results.csv

# agent-eval seeded golden data + per-run scoring output (recreated by setup.sh)
scripts/agent-eval/.golden-data/
scripts/agent-eval/results.json
scripts/agent-eval/examples/results.json

# Smoke-audit screenshots (regenerated on demand)
/landing-desktop.png
/landing-mobile.png
/getting-started.png
/powql.png
/screenshots/

# Fuzz build artifacts + corpus
**/fuzz/target/
**/fuzz/corpus/
**/fuzz/artifacts/

# Local orchestration logs
ITERATION-LOG.md
PRODUCT-REVIEW.md
SPRINT-PLAN.md
SPRINT-LOG.md
AUDIT-REPORT.md

# Secrets and credentials
.env*
*.pem
*.key
*.p12
*.pfx
credentials.json

# Internal-only docs and agent tooling: never publish to the open-source repo.
# Keep these locally / in a private location; they are intentionally untracked.
docs/strategy/
docs/superpowers/
docs/audits/
docs/gtm-strategy.md
.omx/
docs/design/*turbine*.md
